{
    "scripts": {
        "dev": "concurrently --names 'POS' --prefix-colors 'orange,green' --kill-others 'cd frontend/ && bun dev' 'php artisan serve'"
    },
    "devDependencies": {
        "@nuxtjs/color-mode": "^3.4.1"
    },
    "dependencies": {
        "@nuxt/image": "^1.7.0"
    }
}
